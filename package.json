{
  "name": "remote-sync",
  "version": "1.0.2",
  "description": "Upload your files to remote host after every change. Now only scp (sftp) supported, but ftp and webdav is planned.",
  "license": "MIT",
  "repository": "https://github.com/vslinko/remote-sync",
  "main": "index.coffee",
  "engines": {
    "atom": ">=0.78"
  },
  "dependencies": {
    "ssh2": "^0.2",
    "minimatch": "^0.2",
    "async": "^0.2",
    "mkdirp": "^0.3",
    "atom-message-panel": "^1.1.1",
    "fs-plus": "^2.1"
  }
}
